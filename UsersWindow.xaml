<UserControl x:Class="TodoApp.UsersWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TodoApp"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Loaded="WindowLoaded">
    <DockPanel Margin="20">
        <Grid DockPanel.Dock ="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <Button x:Name="SaveButton" Style="{StaticResource PrimaryButton}" Width="100" Click="SaveButton_Click">Зберегти</Button>
                <Button x:Name="CancelButton" Style="{StaticResource PrimaryButton}" Width="100" Margin="20 0 0 0" Click="CancelButton_Click">Відмінити</Button>
            </StackPanel>
            <StackPanel Grid.Column ="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBox x:Name="searchBox" VerticalAlignment="Center" Style="{StaticResource Input}" Width="200" Margin="0 0 20 0"></TextBox>
                <Button x:Name="SearchButton" Style="{StaticResource PrimaryButton}" Width="100" Click="SearchButton_Click">Пошук</Button>
            </StackPanel>
        </Grid>
        <Grid Margin="0 20 0 0">
            <DataGrid x:Name="UsersGrid" AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Width="1*">
                        <DataGridTextColumn.Header>Username</DataGridTextColumn.Header>
                        <DataGridTextColumn.Binding>
                            <Binding Path="username"></Binding>
                        </DataGridTextColumn.Binding>
                    </DataGridTextColumn>
                    <DataGridTextColumn Width="1*">
                        <DataGridTextColumn.Header>Password</DataGridTextColumn.Header>
                        <DataGridTextColumn.Binding>
                            <Binding Path="password"></Binding>
                        </DataGridTextColumn.Binding>
                    </DataGridTextColumn>
                    <DataGridTemplateColumn Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button x:Name="DeleteButton" Click="DeleteButton_Click">Delete</Button>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </DockPanel>
</UserControl>
